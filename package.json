{
  "name": "tabbed-news",
  "version": "1.0.0",
  "description": "a tabbed component to display news items from different sections",
  "main": "main.js",
  "dependencies": {
    "es6-promise": "^4.1.1",
    "fetch-ie8": "^1.5.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "browserify": "^14.4.0",
    "http-server": "^0.10.0"
  },
  "scripts": {
    "build": "babel js/main.js -o dist/main.js && browserify dist/main.js -o dist/main.js",
    "start": "npm run build && http-server"
  },
  "author": "Maël Brunet",
  "license": "ISC"
}
